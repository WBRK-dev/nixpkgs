{
  minimal = {
    path = ./minimal;
    description = "Minimal flake using WBRK-dev/nixpkgs overlay";
  };
  python = {
    path = ./python;
    description = "Python dev shell using WBRK-dev/nixpkgs";
  };
}
